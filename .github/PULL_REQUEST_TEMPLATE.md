<!--
Thanks for opening a PR!

Before you fill this in: ZTXBAS is a closed-source binary distributed
for free. Only the following are open to external contributions:

  - The three SDKs (sdk-go, sdk-node, sdk-python) — Apache-2.0.
  - The quickstarts (quickstarts/) — Apache-2.0.
  - The public docs bundle (docs/).
  - The public API spec (api/openapi.yaml).
  - Anything under deploy/ that's an operator recipe.

PRs against the server binary, the admin console, or any internal
package are not merged here (the source lives elsewhere). If you found
a server bug, please file it as an issue instead.

By opening this PR you agree that your contribution to the SDKs,
quickstarts, or docs is licensed under Apache-2.0 (matching the
directory's existing LICENSE file). No CLA required.
-->

## What this changes

<!-- One sentence on what's changing and why. -->

## Scope

- [ ] SDK (`sdk-go` / `sdk-node` / `sdk-python`)
- [ ] Quickstart
- [ ] Docs (`docs/`)
- [ ] API spec (`api/openapi.yaml`)
- [ ] Deploy recipe (`deploy/`)
- [ ] Other (explain)

## Testing

<!--
For SDK changes: paste the output of the SDK's test command, or say
which cases you added. For docs, note whether you rendered locally.
-->

## Checklist

- [ ] I have read `CONTRIBUTING.md`.
- [ ] My change is limited to the scope listed above (no server-side edits).
- [ ] For SDK changes: existing tests still pass and new tests cover the change where reasonable.
- [ ] For docs: links resolve and code blocks compile/run.
